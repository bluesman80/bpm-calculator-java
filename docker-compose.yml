version: '3.8'

services:
  bpm-calculator:
    build: .
    container_name: bpm-calculator
    ports:
      - "8080:8080"
    environment:
      - RDW_API_TOKEN=${RDW_API_TOKEN}
      - SPRING_PROFILES_ACTIVE=prod
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped